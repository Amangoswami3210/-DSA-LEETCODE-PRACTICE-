
class Solution {
  public:
    // Function to count nodes of a linked list.
    bool searchKey(int n, struct Node* head, int key) {
        
        while(head!=NULL){
            if(key==head->data){
                   return true;
              }
            head=head->next;
        }
        return false;
    }
};
